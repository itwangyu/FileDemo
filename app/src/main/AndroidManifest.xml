<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xtoolapp.file.filedemo">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".MyApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".rebeal.RevealActivity"
            android:theme="@style/noAnimTheme" />
        <activity
            android:name=".xfermode.XfermodeActivity"
            android:hardwareAccelerated="false" />
        <activity android:name=".rebeal.FirstActivity" />
        <activity android:name=".xfermode.sampleActivity" />
        <activity android:name=".file.ScanFileActivity" />
        <activity android:name=".wifi.WifiFileActivity" />
        <activity android:name=".douyin.ListActivity" />
        <activity
            android:name=".douyin.VideoTestActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity android:name=".camera.CameraActivity" />
        <activity android:name=".database.DataBaseActivity" />
    </application>

</manifest>